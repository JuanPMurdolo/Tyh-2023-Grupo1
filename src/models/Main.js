class Main {
    constructor(nodes=[]) {
      this.nodes = nodes;
    }

    newNode(node) {
        this.nodes.push(node);
    }

    broadcast(){
        //to do
    }

    hashMechanismSetUp(){
        //to do
    }

    hashMechanismChange(){
        //to do
    }

  }